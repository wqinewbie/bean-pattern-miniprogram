page {
  height: 100vh;
  background: #FFF9ED;
  overflow: hidden;
}

.standalone-page {
  height: 100vh;
  background: #FFF9ED;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.sub-header {
  position: relative;
  padding: 0 28rpx 12rpx;
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 2rpx solid #FFF4DC;
  background: #FFF9ED;
  flex-shrink: 0;
}

.sub-back {
  font-size: 42rpx;
  color: #8C6B4B;
  width: 96rpx;
  text-align: left;
  z-index: 2;
}

.sub-title-wrap {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  max-width: calc(100% - 240rpx);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.sub-title {
  max-width: 100%;
  font-size: 32rpx;
  font-weight: 800;
  color: #5D4037;
  text-align: center;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.sub-action {
  width: 96rpx;
  text-align: right;
  z-index: 2;
}

.sub-add--hidden {
  visibility: hidden;
}

.sub-scroll {
  flex: 1;
  min-height: 0;
  box-sizing: border-box;
}

.help-page {
  padding: 40rpx 40rpx calc(120rpx + env(safe-area-inset-bottom));
}

.help-section {
  margin-bottom: 48rpx;
}

.help-section-title-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 32rpx;
}

.help-section-bar {
  width: 6rpx;
  height: 40rpx;
  background: #FF9800;
  border-radius: 999rpx;
}

.help-section-title {
  font-size: 32rpx;
  font-weight: 800;
  color: #5D4037;
}

.help-faq-list {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

.help-faq-item {
  background: #fff;
  border-radius: 32rpx;
  padding: 32rpx;
  box-shadow: 0 8rpx 24rpx rgba(140,107,75,0.06);
  border: 4rpx solid #FFF4DC;
}

.help-faq-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 24rpx;
  margin-bottom: 16rpx;
}

.help-faq-question {
  flex: 1;
  font-size: 28rpx;
  font-weight: 800;
  color: #5D4037;
  line-height: 1.5;
}

.help-faq-arrow {
  font-size: 36rpx;
  color: #D0B8A1;
  font-weight: 800;
  flex-shrink: 0;
  margin-top: 4rpx;
}

.help-faq-answer {
  font-size: 26rpx;
  color: #A1887F;
  line-height: 1.6;
  font-weight: 700;
}

.help-contact-card {
  background: linear-gradient(135deg, #FF9800, #FFB74D);
  border-radius: 32rpx;
  padding: 48rpx;
  text-align: center;
  box-shadow: 0 16rpx 48rpx rgba(255,152,0,0.25);
}

.help-contact-title {
  display: block;
  font-size: 32rpx;
  font-weight: 800;
  color: #fff;
  margin-bottom: 16rpx;
}

.help-contact-desc {
  display: block;
  font-size: 26rpx;
  color: rgba(255,255,255,0.9);
  font-weight: 700;
  margin-bottom: 32rpx;
}

.help-contact-btn {
  background: #fff;
  color: #FF9800;
  font-size: 28rpx;
  font-weight: 800;
  padding: 24rpx 48rpx;
  border-radius: 999rpx;
  display: inline-block;
}

.help-contact-btn:active {
  transform: scale(0.95);
}
