<view class="code-overlay-container" style="width: {{width}}px; height: {{height}}px; visibility: {{_visible}};">
  <text
    wx:for="{{labels}}"
    wx:key="key"
    class="code-label"
    style="{{item.style}}"
  >{{item.code}}</text>
</view>
